#include<stdio.h>
int main()
{
    // Check if the num is divisible by 2
    int a;
    printf("Enter a number: ",a);
    scanf("%d", &a);
    printf("%d", a % 2 ==0);

    char star='*';
    printf("%c", star);     //char consists only single values
    return 0;
}